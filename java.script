<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Портфолио</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        /* Загрузка */
        #loading-screen {
            position: fixed;
            width: 100%;
            height: 100%;
            background: white;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        #hoop {
            width: 100px;
            height: 50px;
            border: 5px solid red;
            border-bottom: none;
            margin-bottom: 20px;
        }

        #ball {
            width: 40px;
            height: 40px;
            background: orange;
            border-radius: 50%;
            position: relative;
            animation: miss 1s infinite alternate ease-in-out;
        }

        @keyframes miss {
            0% { transform: translateY(0) translateX(-30px); }
            100% { transform: translateY(-50px) translateX(30px); }
        }

        @keyframes score {
            0% { transform: translateY(-50px) translateX(30px); }
            100% { transform: translateY(50px) translateX(0); opacity: 0; }
        }

        /* Контент сайта (скрыт до загрузки) */
        #content {
            display: none;
            padding: 20px;
        }
    </style>
</head>
<body>

    <!-- Экран загрузки -->
    <div id="loading-screen">
        <div id="hoop"></div>
        <div id="ball"></div>
        <p>Загрузка...</p>
    </div>

    <!-- Основной контент -->
    <div id="content">
        <h1>Добро пожаловать в мое портфолио</h1>
        <p>Здесь будут мои достижения, проекты и контакты.</p>
    </div>

    <script>
        window.addEventListener("load", function() {
            let ball = document.getElementById("ball");
            ball.style.animation = "score 0.5s forwards"; // Мяч залетает

            setTimeout(() => {
                document.getElementById("loading-screen").style.display = "none";
                document.getElementById("content").style.display = "block";
            }, 600);
        });
    </script>

</body>
</html>
